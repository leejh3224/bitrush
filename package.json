{
  "name": "bitrush",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "config": {
    "ecr_image": "678967017970.dkr.ecr.ap-northeast-2.amazonaws.com/bitrush"
  },
  "scripts": {
    "freeze": "pip3 freeze > requirements.txt",
    "test": "python3 -m pytest -s ",
    "ecr:login": "aws ecr get-login-password --profile gompro | docker login --username AWS --password-stdin $npm_package_config_ecr_image",
    "build": "DOCKER_BUILDKIT=1 docker build -t $npm_package_config_ecr_image . --progress=plain",
    "push": "docker push $npm_package_config_ecr_image",
    "ecr:push": "npm run build && npm run push",
    "deploy": "sls deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
